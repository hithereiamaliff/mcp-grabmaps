# GrabMaps MCP Server - Docker Compose
# For self-hosting on VPS
# Requires: docker network create mcp-network

services:
  mcp-grabmaps:
    build: .
    container_name: mcp-grabmaps
    restart: unless-stopped
    ports:
      - "8092:8080"
    environment:
      - PORT=8080
      - HOST=0.0.0.0
      - ANALYTICS_DIR=/app/data
      - NODE_ENV=production
      - KEY_SERVICE_URL=${KEY_SERVICE_URL:-}
      - KEY_SERVICE_TOKEN=${KEY_SERVICE_TOKEN:-}
    volumes:
      - analytics-data:/app/data
      - firebase-credentials:/app/.credentials:ro
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    networks:
      - mcp-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

networks:
  mcp-network:
    external: true

volumes:
  analytics-data:
    driver: local
  firebase-credentials:
    driver: local
