startCommand:
  type: stdio
  configSchema: {}
  commandFunction: |-
    (config) => ({
      "command": "node",
      "args": [
        "dist/smithery.js"
      ],
      "env": {}
    })
build:
  dockerfile: Dockerfile
